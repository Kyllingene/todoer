<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="214" onload="init(evt)" viewBox="0 0 1200 214" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="214" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">Flame Graph</text><text id="details" fill="rgb(0,0,0)" x="10" y="197.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1190" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1190" y="197.00"> </text><svg id="frames" x="10" width="1180" total_samples="27505540"><g><title>&lt;aho_corasick::packed::teddy::builder::x86_64::SlimAVX2&lt;3_usize&gt; as aho_corasick::packed::teddy::builder::SearcherT&gt;::find (2,187,223 samples, 7.95%)</title><rect x="0.0000%" y="133" width="7.9519%" height="15" fill="rgb(227,0,7)" fg:x="0" fg:w="2187223"/><text x="0.2500%" y="143.50">&lt;aho_corasi..</text></g><g><title>aho_corasick::packed::teddy::generic::Slim&lt;V,3_usize&gt;::find (2,187,223 samples, 7.95%)</title><rect x="0.0000%" y="117" width="7.9519%" height="15" fill="rgb(217,0,24)" fg:x="0" fg:w="2187223"/><text x="0.2500%" y="127.50">aho_corasic..</text></g><g><title>aho_corasick::packed::teddy::generic::Slim&lt;V,3_usize&gt;::find_one (1,017,539 samples, 3.70%)</title><rect x="4.2525%" y="101" width="3.6994%" height="15" fill="rgb(221,193,54)" fg:x="1169684" fg:w="1017539"/><text x="4.5025%" y="111.50">aho_..</text></g><g><title>aho_corasick::packed::teddy::generic::Slim&lt;V,3_usize&gt;::candidate (1,017,539 samples, 3.70%)</title><rect x="4.2525%" y="85" width="3.6994%" height="15" fill="rgb(248,212,6)" fg:x="1169684" fg:w="1017539"/><text x="4.5025%" y="95.50">aho_..</text></g><g><title>aho_corasick::packed::teddy::generic::Mask&lt;V&gt;::members3 (1,017,539 samples, 3.70%)</title><rect x="4.2525%" y="69" width="3.6994%" height="15" fill="rgb(208,68,35)" fg:x="1169684" fg:w="1017539"/><text x="4.5025%" y="79.50">aho_..</text></g><g><title>aho_corasick::packed::vector::x86_64_ssse3::&lt;impl aho_corasick::packed::vector::Vector for core::core_arch::x86::__m128i&gt;::shuffle_bytes (1,017,539 samples, 3.70%)</title><rect x="4.2525%" y="53" width="3.6994%" height="15" fill="rgb(232,128,0)" fg:x="1169684" fg:w="1017539"/><text x="4.5025%" y="63.50">aho_..</text></g><g><title>core::core_arch::x86::ssse3::_mm_shuffle_epi8 (1,017,539 samples, 3.70%)</title><rect x="4.2525%" y="37" width="3.6994%" height="15" fill="rgb(207,160,47)" fg:x="1169684" fg:w="1017539"/><text x="4.5025%" y="47.50">core..</text></g><g><title>__memcmp_avx2_movbe (1,165,444 samples, 4.24%)</title><rect x="7.9519%" y="133" width="4.2371%" height="15" fill="rgb(228,23,34)" fg:x="2187223" fg:w="1165444"/><text x="8.2019%" y="143.50">__mem..</text></g><g><title>_int_malloc (2,333,870 samples, 8.49%)</title><rect x="12.1891%" y="133" width="8.4851%" height="15" fill="rgb(218,30,26)" fg:x="3352667" fg:w="2333870"/><text x="12.4391%" y="143.50">_int_malloc</text></g><g><title>_start (1,091,072 samples, 3.97%)</title><rect x="20.6742%" y="133" width="3.9667%" height="15" fill="rgb(220,122,19)" fg:x="5686537" fg:w="1091072"/><text x="20.9242%" y="143.50">_sta..</text></g><g><title>aho_corasick::dfa::Builder::build_from_noncontiguous (1,224,657 samples, 4.45%)</title><rect x="24.6409%" y="133" width="4.4524%" height="15" fill="rgb(250,228,42)" fg:x="6777609" fg:w="1224657"/><text x="24.8909%" y="143.50">aho_c..</text></g><g><title>aho_corasick::dfa::Builder::finish_build_one_start (1,224,657 samples, 4.45%)</title><rect x="24.6409%" y="117" width="4.4524%" height="15" fill="rgb(240,193,28)" fg:x="6777609" fg:w="1224657"/><text x="24.8909%" y="127.50">aho_c..</text></g><g><title>aho_corasick::dfa::sparse_iter (1,224,657 samples, 4.45%)</title><rect x="24.6409%" y="101" width="4.4524%" height="15" fill="rgb(216,20,37)" fg:x="6777609" fg:w="1224657"/><text x="24.8909%" y="111.50">aho_c..</text></g><g><title>core::cmp::PartialEq::ne (1,224,657 samples, 4.45%)</title><rect x="24.6409%" y="85" width="4.4524%" height="15" fill="rgb(206,188,39)" fg:x="6777609" fg:w="1224657"/><text x="24.8909%" y="95.50">core:..</text></g><g><title>&lt;core::option::Option&lt;T&gt; as core::cmp::PartialEq&gt;::eq (1,224,657 samples, 4.45%)</title><rect x="24.6409%" y="69" width="4.4524%" height="15" fill="rgb(217,207,13)" fg:x="6777609" fg:w="1224657"/><text x="24.8909%" y="79.50">&lt;core..</text></g><g><title>aho_corasick::nfa::noncontiguous::Compiler::add_unanchored_start_state_loop (1,309,013 samples, 4.76%)</title><rect x="29.0933%" y="133" width="4.7591%" height="15" fill="rgb(231,73,38)" fg:x="8002266" fg:w="1309013"/><text x="29.3433%" y="143.50">aho_co..</text></g><g><title>aho_corasick::packed::rabinkarp::RabinKarp::find_at (1,197,522 samples, 4.35%)</title><rect x="33.8524%" y="133" width="4.3537%" height="15" fill="rgb(225,20,46)" fg:x="9311279" fg:w="1197522"/><text x="34.1024%" y="143.50">aho_c..</text></g><g><title>aho_corasick::packed::rabinkarp::RabinKarp::verify (1,045,660 samples, 3.80%)</title><rect x="38.2061%" y="133" width="3.8016%" height="15" fill="rgb(210,31,41)" fg:x="10508801" fg:w="1045660"/><text x="38.4561%" y="143.50">aho_..</text></g><g><title>aho_corasick::packed::pattern::Patterns::get (1,045,660 samples, 3.80%)</title><rect x="38.2061%" y="117" width="3.8016%" height="15" fill="rgb(221,200,47)" fg:x="10508801" fg:w="1045660"/><text x="38.4561%" y="127.50">aho_..</text></g><g><title>aho_corasick::util::primitives::&lt;impl core::ops::index::Index&lt;aho_corasick::util::primitives::PatternID&gt; for alloc::vec::Vec&lt;T&gt;&gt;::index (1,045,660 samples, 3.80%)</title><rect x="38.2061%" y="101" width="3.8016%" height="15" fill="rgb(226,26,5)" fg:x="10508801" fg:w="1045660"/><text x="38.4561%" y="111.50">aho_..</text></g><g><title>aho_corasick::util::primitives::PatternID::as_usize (1,045,660 samples, 3.80%)</title><rect x="38.2061%" y="85" width="3.8016%" height="15" fill="rgb(249,33,26)" fg:x="10508801" fg:w="1045660"/><text x="38.4561%" y="95.50">aho_..</text></g><g><title>aho_corasick::util::primitives::SmallIndex::as_usize (1,045,660 samples, 3.80%)</title><rect x="38.2061%" y="69" width="3.8016%" height="15" fill="rgb(235,183,28)" fg:x="10508801" fg:w="1045660"/><text x="38.4561%" y="79.50">aho_..</text></g><g><title>alloc::raw_vec::finish_grow (1,059,682 samples, 3.85%)</title><rect x="42.0078%" y="133" width="3.8526%" height="15" fill="rgb(221,5,38)" fg:x="11554461" fg:w="1059682"/><text x="42.2578%" y="143.50">allo..</text></g><g><title>core::result::Result&lt;T,E&gt;::map_err (1,059,682 samples, 3.85%)</title><rect x="42.0078%" y="117" width="3.8526%" height="15" fill="rgb(247,18,42)" fg:x="11554461" fg:w="1059682"/><text x="42.2578%" y="127.50">core..</text></g><g><title>core::slice::memchr::memchr_aligned (2,020,437 samples, 7.35%)</title><rect x="45.8604%" y="133" width="7.3456%" height="15" fill="rgb(241,131,45)" fg:x="12614143" fg:w="2020437"/><text x="46.1104%" y="143.50">core::slic..</text></g><g><title>core::slice::memchr::memchr_naive (998,669 samples, 3.63%)</title><rect x="49.5751%" y="117" width="3.6308%" height="15" fill="rgb(249,31,29)" fg:x="13635911" fg:w="998669"/><text x="49.8251%" y="127.50">core..</text></g><g><title>globset::GlobSet::matches_candidate_into (1,146,395 samples, 4.17%)</title><rect x="53.2059%" y="133" width="4.1679%" height="15" fill="rgb(225,111,53)" fg:x="14634580" fg:w="1146395"/><text x="53.4559%" y="143.50">globs..</text></g><g><title>globset::GlobSetMatchStrategy::matches_into (1,146,395 samples, 4.17%)</title><rect x="53.2059%" y="117" width="4.1679%" height="15" fill="rgb(238,160,17)" fg:x="14634580" fg:w="1146395"/><text x="53.4559%" y="127.50">globs..</text></g><g><title>globset::RegexSetStrategy::matches_into (1,146,395 samples, 4.17%)</title><rect x="53.2059%" y="101" width="4.1679%" height="15" fill="rgb(214,148,48)" fg:x="14634580" fg:w="1146395"/><text x="53.4559%" y="111.50">globs..</text></g><g><title>regex::regexset::bytes::RegexSet::matches (1,146,395 samples, 4.17%)</title><rect x="53.2059%" y="85" width="4.1679%" height="15" fill="rgb(232,36,49)" fg:x="14634580" fg:w="1146395"/><text x="53.4559%" y="95.50">regex..</text></g><g><title>regex::regexset::bytes::RegexSet::matches_at (1,146,395 samples, 4.17%)</title><rect x="53.2059%" y="69" width="4.1679%" height="15" fill="rgb(209,103,24)" fg:x="14634580" fg:w="1146395"/><text x="53.4559%" y="79.50">regex..</text></g><g><title>regex_automata::meta::regex::Regex::which_overlapping_matches (1,146,395 samples, 4.17%)</title><rect x="53.2059%" y="53" width="4.1679%" height="15" fill="rgb(229,88,8)" fg:x="14634580" fg:w="1146395"/><text x="53.4559%" y="63.50">regex..</text></g><g><title>&lt;alloc::sync::Arc&lt;T,A&gt; as core::ops::deref::Deref&gt;::deref (1,146,395 samples, 4.17%)</title><rect x="53.2059%" y="37" width="4.1679%" height="15" fill="rgb(213,181,19)" fg:x="14634580" fg:w="1146395"/><text x="53.4559%" y="47.50">&lt;allo..</text></g><g><title>ignore::dir::Ignore::add_child_path (990,797 samples, 3.60%)</title><rect x="57.3738%" y="133" width="3.6022%" height="15" fill="rgb(254,191,54)" fg:x="15780975" fg:w="990797"/><text x="57.6238%" y="143.50">igno..</text></g><g><title>ignore::dir::resolve_git_commondir (990,797 samples, 3.60%)</title><rect x="57.3738%" y="117" width="3.6022%" height="15" fill="rgb(241,83,37)" fg:x="15780975" fg:w="990797"/><text x="57.6238%" y="127.50">igno..</text></g><g><title>ignore::gitignore::Gitignore::matched_stripped (1,056,066 samples, 3.84%)</title><rect x="60.9760%" y="133" width="3.8395%" height="15" fill="rgb(233,36,39)" fg:x="16771772" fg:w="1056066"/><text x="61.2260%" y="143.50">igno..</text></g><g><title>thread_local::ThreadLocal&lt;T&gt;::get_or_default (1,056,066 samples, 3.84%)</title><rect x="60.9760%" y="117" width="3.8395%" height="15" fill="rgb(226,3,54)" fg:x="16771772" fg:w="1056066"/><text x="61.2260%" y="127.50">thre..</text></g><g><title>thread_local::ThreadLocal&lt;T&gt;::get_or (1,056,066 samples, 3.84%)</title><rect x="60.9760%" y="101" width="3.8395%" height="15" fill="rgb(245,192,40)" fg:x="16771772" fg:w="1056066"/><text x="61.2260%" y="111.50">thre..</text></g><g><title>thread_local::ThreadLocal&lt;T&gt;::get_or_try (1,056,066 samples, 3.84%)</title><rect x="60.9760%" y="85" width="3.8395%" height="15" fill="rgb(238,167,29)" fg:x="16771772" fg:w="1056066"/><text x="61.2260%" y="95.50">thre..</text></g><g><title>thread_local::ThreadLocal&lt;T&gt;::get_inner (1,056,066 samples, 3.84%)</title><rect x="60.9760%" y="69" width="3.8395%" height="15" fill="rgb(232,182,51)" fg:x="16771772" fg:w="1056066"/><text x="61.2260%" y="79.50">thre..</text></g><g><title>malloc_consolidate (1,343,206 samples, 4.88%)</title><rect x="64.8154%" y="133" width="4.8834%" height="15" fill="rgb(231,60,39)" fg:x="17827838" fg:w="1343206"/><text x="65.0654%" y="143.50">malloc..</text></g><g><title>memchr::arch::x86_64::avx2::memchr::One::rfind_raw_avx2 (1,330,650 samples, 4.84%)</title><rect x="69.6988%" y="133" width="4.8378%" height="15" fill="rgb(208,69,12)" fg:x="19171044" fg:w="1330650"/><text x="69.9488%" y="143.50">memchr..</text></g><g><title>memchr::arch::generic::memchr::One&lt;V&gt;::rfind_raw (1,330,650 samples, 4.84%)</title><rect x="69.6988%" y="117" width="4.8378%" height="15" fill="rgb(235,93,37)" fg:x="19171044" fg:w="1330650"/><text x="69.9488%" y="127.50">memchr..</text></g><g><title>regex_automata::hybrid::search::find_overlapping_fwd (1,068,449 samples, 3.88%)</title><rect x="74.5366%" y="133" width="3.8845%" height="15" fill="rgb(213,116,39)" fg:x="20501694" fg:w="1068449"/><text x="74.7866%" y="143.50">rege..</text></g><g><title>regex_automata::hybrid::search::find_overlapping_fwd_imp (1,068,449 samples, 3.88%)</title><rect x="74.5366%" y="117" width="3.8845%" height="15" fill="rgb(222,207,29)" fg:x="20501694" fg:w="1068449"/><text x="74.7866%" y="127.50">rege..</text></g><g><title>regex_automata::hybrid::dfa::Cache::search_start (1,068,449 samples, 3.88%)</title><rect x="74.5366%" y="101" width="3.8845%" height="15" fill="rgb(206,96,30)" fg:x="20501694" fg:w="1068449"/><text x="74.7866%" y="111.50">rege..</text></g><g><title>regex_automata::meta::regex::Builder::configure (1,128,157 samples, 4.10%)</title><rect x="78.4211%" y="133" width="4.1016%" height="15" fill="rgb(218,138,4)" fg:x="21570143" fg:w="1128157"/><text x="78.6711%" y="143.50">rege..</text></g><g><title>std::fs::read_to_string::inner (1,080,462 samples, 3.93%)</title><rect x="82.5226%" y="133" width="3.9282%" height="15" fill="rgb(250,191,14)" fg:x="22698300" fg:w="1080462"/><text x="82.7726%" y="143.50">std:..</text></g><g><title>std::io::default_read_to_end (1,068,570 samples, 3.88%)</title><rect x="86.4508%" y="133" width="3.8849%" height="15" fill="rgb(239,60,40)" fg:x="23778762" fg:w="1068570"/><text x="86.7008%" y="143.50">std:..</text></g><g><title>core::cmp::min (1,068,570 samples, 3.88%)</title><rect x="86.4508%" y="117" width="3.8849%" height="15" fill="rgb(206,27,48)" fg:x="23778762" fg:w="1068570"/><text x="86.7008%" y="127.50">core..</text></g><g><title>core::cmp::Ord::min (1,068,570 samples, 3.88%)</title><rect x="86.4508%" y="101" width="3.8849%" height="15" fill="rgb(225,35,8)" fg:x="23778762" fg:w="1068570"/><text x="86.7008%" y="111.50">core..</text></g><g><title>core::cmp::min_by (1,068,570 samples, 3.88%)</title><rect x="86.4508%" y="85" width="3.8849%" height="15" fill="rgb(250,213,24)" fg:x="23778762" fg:w="1068570"/><text x="86.7008%" y="95.50">core..</text></g><g><title>&lt;core::iter::adapters::enumerate::Enumerate&lt;I&gt; as core::iter::traits::iterator::Iterator&gt;::next (1,205,789 samples, 4.38%)</title><rect x="90.3357%" y="117" width="4.3838%" height="15" fill="rgb(247,123,22)" fg:x="24847332" fg:w="1205789"/><text x="90.5857%" y="127.50">&lt;core..</text></g><g><title>&lt;core::str::iter::Lines as core::iter::traits::iterator::Iterator&gt;::next (1,205,789 samples, 4.38%)</title><rect x="90.3357%" y="101" width="4.3838%" height="15" fill="rgb(231,138,38)" fg:x="24847332" fg:w="1205789"/><text x="90.5857%" y="111.50">&lt;core..</text></g><g><title>&lt;core::iter::adapters::map::Map&lt;I,F&gt; as core::iter::traits::iterator::Iterator&gt;::next (1,205,789 samples, 4.38%)</title><rect x="90.3357%" y="85" width="4.3838%" height="15" fill="rgb(231,145,46)" fg:x="24847332" fg:w="1205789"/><text x="90.5857%" y="95.50">&lt;core..</text></g><g><title>&lt;core::str::iter::SplitInclusive&lt;P&gt; as core::iter::traits::iterator::Iterator&gt;::next (1,205,789 samples, 4.38%)</title><rect x="90.3357%" y="69" width="4.3838%" height="15" fill="rgb(251,118,11)" fg:x="24847332" fg:w="1205789"/><text x="90.5857%" y="79.50">&lt;core..</text></g><g><title>core::str::iter::SplitInternal&lt;P&gt;::next_inclusive (1,205,789 samples, 4.38%)</title><rect x="90.3357%" y="53" width="4.3838%" height="15" fill="rgb(217,147,25)" fg:x="24847332" fg:w="1205789"/><text x="90.5857%" y="63.50">core:..</text></g><g><title>&lt;core::str::pattern::CharSearcher as core::str::pattern::Searcher&gt;::next_match (1,205,789 samples, 4.38%)</title><rect x="90.3357%" y="37" width="4.3838%" height="15" fill="rgb(247,81,37)" fg:x="24847332" fg:w="1205789"/><text x="90.5857%" y="47.50">&lt;core..</text></g><g><title>todoer::main (2,286,613 samples, 8.31%)</title><rect x="90.3357%" y="133" width="8.3133%" height="15" fill="rgb(209,12,38)" fg:x="24847332" fg:w="2286613"/><text x="90.5857%" y="143.50">todoer::main</text></g><g><title>core::ptr::drop_in_place&lt;regex::regex::string::Matches&gt; (1,080,824 samples, 3.93%)</title><rect x="94.7195%" y="117" width="3.9295%" height="15" fill="rgb(227,1,9)" fg:x="26053121" fg:w="1080824"/><text x="94.9695%" y="127.50">core..</text></g><g><title>core::ptr::drop_in_place&lt;regex_automata::meta::regex::FindMatches&gt; (1,080,824 samples, 3.93%)</title><rect x="94.7195%" y="101" width="3.9295%" height="15" fill="rgb(248,47,43)" fg:x="26053121" fg:w="1080824"/><text x="94.9695%" y="111.50">core..</text></g><g><title>all (27,505,540 samples, 100%)</title><rect x="0.0000%" y="165" width="100.0000%" height="15" fill="rgb(221,10,30)" fg:x="0" fg:w="27505540"/><text x="0.2500%" y="175.50"></text></g><g><title>todoer (27,505,540 samples, 100.00%)</title><rect x="0.0000%" y="149" width="100.0000%" height="15" fill="rgb(210,229,1)" fg:x="0" fg:w="27505540"/><text x="0.2500%" y="159.50">todoer</text></g><g><title>update_active (371,595 samples, 1.35%)</title><rect x="98.6490%" y="133" width="1.3510%" height="15" fill="rgb(222,148,37)" fg:x="27133945" fg:w="371595"/><text x="98.8990%" y="143.50"></text></g></svg></svg>